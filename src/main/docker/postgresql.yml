version: '2'
services:
  pradera-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/pradera/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=pradera
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
